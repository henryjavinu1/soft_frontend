import 'package:flutter/material.dart';

class PrincipalMantenimiento extends StatelessWidget {
  const PrincipalMantenimiento({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ventana Principal de mantenimiento'),
      ),
    );
  }
}
